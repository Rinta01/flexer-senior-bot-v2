# Быстрый старт - Флексер старший

## Пошаговое руководство для запуска бота

### Шаг 1: Подготовка окружения

```bash
# 1. Клонировать репозиторий (если ещё не клонировали)
git clone https://github.com/Rinta01/flexer-senior-bot-v2.git
cd flexer-senior-bot-v2

# 2. Создать виртуальное окружение
python3.12 -m venv venv
source venv/bin/activate

# 3. Обновить pip
pip install --upgrade pip

# 4. Установить зависимости (с dev и db)
pip install -e ".[dev,db]"
```

### Шаг 2: Получить токен бота

1. Откройте [BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям BotFather
4. Скопируйте токен бота (выглядит как `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### Шаг 3: Конфигурация

#### Вариант A: Локальное развертывание (SQLite)

```bash
# 1. Скопировать .env и отредактировать
cp .env.example .env

# 2. Отредактировать .env файл
nano .env  # или используйте свой редактор

# Установить:
BOT_TOKEN=<ваш_токен_от_BotFather>
DATABASE_URL=sqlite+aiosqlite:///./flexer_senior.db
LOG_LEVEL=INFO
```

#### Вариант B: Docker развертывание (рекомендуется)

```bash
# 1. Установить Docker и Docker Compose
# macOS: brew install docker
# Linux: sudo apt-get install docker.io docker-compose
# Windows: https://www.docker.com/products/docker-desktop

# 2. Скопировать и отредактировать .env
cp .env.example .env

# Установить в .env:
BOT_TOKEN=<ваш_токен_от_BotFather>
LOG_LEVEL=INFO
ENVIRONMENT=development

# 3. Запустить с Docker Compose
docker-compose up

# Готово! Бот запущен с SQLite в контейнере
# База данных хранится в томе ./data
```

### Шаг 4: Добавить бота в группу

1. Откройте или создайте Telegram группу
2. Добавьте вашего бота в группу
3. Дайте боту права администратора (опционально, но рекомендуется)

### Шаг 5: Первый тест

В группе отправьте:

```
/start
```

Бот должен ответить приветствием.

Попробуйте команды:

```
/join         # Присоединиться к пулу
/pool         # Показать участников
/force_pick   # Выбрать дежурного вручную
```

После выбора дежурного, назначенный пользователь увидит кнопки для подтверждения или отказа.

## 🧪 Запуск тестов

### Локально

```bash
# Все тесты
pytest

# С отчётом покрытия
pytest --cov=src

# HTML отчёт
pytest --cov=src --cov-report=html
open htmlcov/index.html
```

### С Docker

```bash
# Создать контейнер для тестов
docker build -t flexer-bot-test .

# Запустить тесты
docker run flexer-bot-test pytest
```

## 📝 Командные переменные планировщика

Настроить, когда бот выбирает дежурного:

```bash
# В .env файле:

# День недели (0=Пн, 1=Вт, ..., 6=Вс)
WEEKLY_DUTY_DAY=0

# Час (0-23)
WEEKLY_DUTY_HOUR=10

# Минута (0-59)
WEEKLY_DUTY_MINUTE=0

# Результат: Каждый понедельник в 10:00 UTC
```

## 🔍 Проверка статуса

### Логи бота (Docker)

```bash
docker-compose logs -f bot
```

### Логи бота (локально)

```bash
# Просмотр в консоли при запуске
python -m src.bot
```

## 🛠️ Основные команды для разработки

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Проверить типы с mypy
mypy src/

# Линтинг с ruff
ruff check src/

# Форматирование с black
black src/

# Запустить бота
python -m src.bot

# Остановить бота
# Ctrl + C (в консоли)
```

## 🐛 Частые проблемы и решения

### "ModuleNotFoundError: No module named 'aiogram'"

```bash
# Решение: переинсталлировать зависимости
pip install -e ".[dev,db]"
```

### "Ошибка подключения к БД"

```bash
# Решение 1: Проверить что база данных существует
ls -la flexer_senior.db

# Решение 2: Проверить DATABASE_URL в .env
echo $DATABASE_URL

# Решение 3: Переинициализировать БД (локально)
rm flexer_senior.db
python -m src.bot  # Будет создана новая БД
```

createdb flexer_senior_db

````

### "База данных не существует"

```bash
# SQLite автоматически создаст файл базы данных при первом запуске
# Путь: ./flexer_senior.db
````

### "BOT_TOKEN не установлен"

```bash
# Решение: Проверить .env файл
cat .env | grep BOT_TOKEN

# Если пусто, добавить:
echo "BOT_TOKEN=123456:ABC-DEF1234..." >> .env
```

## 📚 Дальнейшее развитие

### Добавить свою команду

1. Создать обработчик в `src/handlers/my_command.py`
2. Зарегистрировать в `src/bot.py`
3. Написать тесты в `tests/unit/test_my_command.py`

### Добавить бизнес-логику

1. Создать сервис в `src/services/`
2. Использовать репозитории для доступа к БД
3. Вызвать из обработчика

## 📞 Получить помощь

- Прочитайте [README.md](README.md) для полной документации
- Смотрите примеры в `src/handlers/`
- Запустите тесты для понимания API: `pytest -v`

---

**Удачи! Если что-то не работает - создайте Issue на GitHub** 🚀
