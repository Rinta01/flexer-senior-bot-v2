# Флексер старший - Telegram бот для управления дежурными

![Python](https://img.shields.io/badge/Python-3.12+-blue)
![aiogram](https://img.shields.io/badge/aiogram-3.24+-green)
![License](https://img.shields.io/badge/License-MIT-yellow)
![Tests](https://img.shields.io/badge/Tests-27_passing-success)

Telegram-бот для автоматического управления ротацией "дежурных" в дружеских группах. Бот еженедельно выбирает дежурного из пула участников, обеспечивает систему подтверждений и управление активностями.

## 📋 Основные возможности

- ✅ **Автоматическое назначение дежурных** - Еженедельный выбор случайного дежурного
- ✅ **Система подтверждения** - Дежурный принимает или отказывается через inline-кнопки
- ✅ **Управление активностями** - Дежурный может установить мероприятие на неделю
- ✅ **Безопасность** - Только подтвержденный дежурный текущей недели устанавливает активность
- ✅ **Умная ротация** - Гарантия отсутствия повторений в цикле
- ✅ **Статусы и диапазоны дат** - Понятное отображение информации
- ✅ **Полное покрытие тестами** - 27+ unit и интеграционных тестов

## 🚀 Быстрый старт

### Требования

- Python 3.12+
- SQLite 3+
- Telegram бот токен от [@BotFather](https://t.me/botfather)

### Установка

```bash
# 1. Клонировать репозиторий
git clone https://github.com/Rinta01/flexer-senior-bot-v2.git
cd flexer-senior-bot-v2

# 2. Создать виртуальное окружение
python3.12 -m venv venv
source venv/bin/activate  # Linux/Mac

# 3. Установить зависимости
pip install -e ".[dev,db]"

# 4. Настроить конфигурацию
cp .env.example .env
# Отредактировать .env и установить BOT_TOKEN

# 5. Запустить бота
python -m src.bot
```

### Docker Compose

```bash
# 1. Настроить .env
cp .env.example .env
# Установить BOT_TOKEN

# 2. Запустить
docker-compose up -d
```

## 📖 Команды

| Команда                 | Описание                                                      |
| ----------------------- | ------------------------------------------------------------- |
| `/start`                | Показать приветствие                                          |
| `/help`                 | Полная справка по командам                                    |
| `/join`                 | Присоединиться к пулу дежурных                                |
| `/leave`                | Выйти из пула дежурных                                        |
| `/pool`                 | Показать список участников                                    |
| `/activity`             | Показать дежурного и активность недели                        |
| `/set_activity`         | Установить мероприятие (только подтвержденный дежурный)       |
| `/force_pick @username` | Назначить конкретного пользователя дежурным на текущую неделю |

### Пример установки активности

```
/set_activity Игра в мафию | Играем в кафе Пушкин | 15.01.2026 | 19:30
```

## 📚 Полная документация

**➡️ [DOCUMENTATION.md](DOCUMENTATION.md)** - Полная документация со всеми деталями

Включает:

- Подробное описание всех команд
- Система активностей и безопасность
- Система подтверждений дежурств
- Архитектура и структура проекта
- Руководство по разработке
- Deployment и конфигурация

## 🧪 Тестирование

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=src --cov-report=html
```

**Текущее состояние:** 27/27 тестов проходят ✅

## 🏗️ Архитектура

```

src/
├── bot.py # Главное приложение
├── config.py # Конфигурация
├── handlers/ # Обработчики команд
│ ├── start.py
│ ├── join.py
│ ├── leave.py
│ └── duty.py
├── services/ # Бизнес-логика
│ ├── duty_manager.py # Управление ротацией
│ ├── user_manager.py # Управление пулом
│ └── notification.py # Отправка уведомлений
├── database/ # Слой данных
│ ├── models.py # SQLAlchemy модели
│ ├── engine.py # Управление БД
│ └── repositories.py # CRUD операции
├── middlewares/ # Асинхронные middlewares
└── utils/ # Утилиты
├── logger.py
└── validators.py

```

## 🗄️ Модели данных

### TelegramUser

```python
- user_id (BigInteger, unique)
- username (String, optional)
- first_name (String)
- last_name (String, optional)
- is_active (Boolean)
```

### DutyPool

```python
- group_id (BigInteger, unique)
- group_title (String)
- current_cycle (Integer)
- is_active (Boolean)
```

### DutyAssignment

```python
- user_id (ForeignKey)
- pool_id (ForeignKey)
- week_number (Integer)
- assignment_date (DateTime)
- cycle_number (Integer)
- notification_sent (Boolean)
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
pytest

# С отчётом покрытия
pytest --cov=src

# HTML отчёт
pytest --cov=src --cov-report=html
open htmlcov/index.html
```

### Структура тестов

```
tests/
├── conftest.py                # Фикстуры
├── unit/                      # Unit тесты
│   ├── test_duty_manager.py
│   ├── test_user_manager.py
│   ├── test_validators.py
│   └── test_repositories.py
└── integration/               # Интеграционные тесты
    └── test_handlers.py
```

## 🔄 Логика выбора дежурного

1. **Получить пул активных пользователей** группы
2. **Исключить уже дежуривших** в текущем цикле
3. **Случайный выбор** из оставшихся
4. **При завершении цикла** - сброс и начало нового раунда

```python
# Пример: 3 пользователя, цикл 1
Цикл 1:  Alice -> Bob -> Charlie
Цикл 2:  Bob -> Charlie -> Alice
```

## 📊 Примеры использования

### Присоединение к пулу

```
User: /join
Bot: ✅ Вы присоединились к пулу дежурных!
Bot: 👥 Участников в пуле: 5
```

### Объявление дежурного

```
Bot: 🎯 Дежурный на неделю #2
Bot: Поздравляем, @alice! 🎉
Bot: Ты назначен дежурным на эту неделю...
```

## 🔧 Конфигурация планировщика

Бот использует **APScheduler** для еженедельного выбора дежурного:

```python
# Пример: Каждый понедельник в 10:00 UTC

WEEKLY_DUTY_DAY=0      # Понедельник (0=Пн, 6=Вс)
WEEKLY_DUTY_HOUR=10    # Час
WEEKLY_DUTY_MINUTE=0   # Минута
```

## 📝 Логирование

Логи выводятся в консоль с уровнем, заданным в `LOG_LEVEL`:

```
[2024-01-09 10:15:30] flexer_senior - INFO - Started polling...
[2024-01-09 10:15:45] flexer_senior - INFO - User joined pool
```

## 🐛 Обработка ошибок

Все операции обёрнуты в try/except с логированием:

```python
try:
    # Операция БД или API
except Exception as e:
    logger.error(f"Error: {e}")
    # Возврат пользователю читаемого сообщения об ошибке
```

## 📦 Развертывание

### Локально (Development)

```bash
# Запустить локально
python -m src.bot
```

### Production с Docker

```bash
# Собрать образ
docker build -t flexer-senior-bot .

# Запустить контейнер
docker run -d \
  --env-file .env \
  --name flexer-bot \
  flexer-senior-bot
```

### Production на сервере

1. Скопировать проект на сервер
2. Использовать systemd/supervisor для управления процессом
3. Настроить логирование (systemd journal или файлы)

## 🤝 Участие в разработке

```bash
# Создать feature branch
git checkout -b feature/your-feature

# Написать тесты
pytest

# Проверить качество кода
ruff check src/
mypy src/

# Коммит и push
git commit -am "feat: description"
git push origin feature/your-feature
```

## 📄 Лицензия

MIT License - см. файл LICENSE

## 📞 Поддержка

Обнаружили ошибку? [Создайте Issue](https://github.com/Rinta01/flexer-senior-bot-v2/issues)

## 🎯 Будущие улучшения

- [ ] Web интерфейс администратора
- [ ] Поддержка Discord
- [ ] Кастомные расписания для каждой группы
- [ ] Статистика дежурных (график)
- [ ] Уведомления за день до дежурства
- [ ] Причины отсутствия дежурного

---

**Создано с ❤️ для управления дежурными**
